[package]
name = "ocp_vehicle_example"
version = "0.1.0"
authors = ["Voysys AB <support@voysys.se>"]
edition = "2024"
license = "0BSD"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["net", "sync", "io-util", "macros", "rt-multi-thread", "time"]}

[workspace]
members = []